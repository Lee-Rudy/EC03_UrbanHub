services:
  ms-alerte:
    build: .
    ports:
      - "8005:8005"
    environment:
      MONGO_URI: mongodb://mongo:27017/alerte_db
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: